# Frontend Environment Variables (for React)

# URL of your backend API
# For local development (if backend runs on port 8000): REACT_APP_BACKEND_URL=http://localhost:8000/api
# For production: REACT_APP_BACKEND_URL=https://your-live-backend-url.com/api
REACT_APP_BACKEND_URL=

# Worldcoin App ID (from Worldcoin Developer Portal)
REACT_APP_WORLDCOIN_APP_ID=app_your_worldcoin_app_id

# Worldcoin Action Name (must match backend if verified there too)
REACT_APP_WORLDCOIN_ACTION_NAME=banka_wallet_login

# Optional: Set to 'production' for Netlify builds, or 'development' locally
NODE_ENV=development
