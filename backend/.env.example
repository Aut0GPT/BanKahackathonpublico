# Backend Environment Variables (for FastAPI Server)

# URL of your deployed frontend application
# For local development (if frontend runs on port 3000): FRONTEND_URL=http://localhost:3000
# For production: FRONTEND_URL=https://banka-wallet-app.windsurf.build
FRONTEND_URL=

# MongoDB Connection URI
# Example: MONGODB_URI=mongodb://user:password@host:port/database
MONGODB_URI=

# Web3 Provider URL (e.g., Infura, Alchemy, or your own node for BNB Testnet)
# Example for BNB Testnet public RPC: WEB3_PROVIDER_URL=https://data-seed-prebsc-1-s1.binance.org:8545/
WEB3_PROVIDER_URL=

# Wallet Mnemonic for backend operations (VERY SENSITIVE - DO NOT COMMIT ACTUAL MNEMONIC TO PUBLIC REPO)
WALLET_MNEMONIC="your twelve word test mnemonic phrase here for development only"

# JWT Secret Key for signing tokens (SENSITIVE)
JWT_SECRET_KEY=your-super-secret-jwt-key-that-is-long-and-random

# Deployed EventFactory Contract Address on BNB Testnet
EVENT_FACTORY_ADDRESS=

# BNB Testnet Chain ID (e.g., 97)
BNB_TESTNET_CHAIN_ID=97

# Optional: Worldcoin App ID (if backend needs to verify it directly)
# WORLDCOIN_APP_ID=app_your_worldcoin_app_id

# Optional: Worldcoin Action Name (if backend needs to verify it directly)
# WORLDCOIN_ACTION_NAME=banka_wallet_login

# Python environment (development, production, testing)
# PYTHON_ENV=development
